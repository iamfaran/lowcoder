import { css } from "styled-components";
import { TableRowStyleType } from "comps/controls/styleControlConstants";

export const getTableRowStyles = (
  rowStyle: TableRowStyleType
) => css`
  /* TODO: Implement row styles here */
  /* You can follow the pattern from main table's getStyle function */
  /* This should include: background, alternateBackground, selectedRowBackground, hoverRowBackground, etc. */
`; 